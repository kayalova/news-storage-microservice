version: "3.9"
services:
  postgres:
    image: postgres:14.5
    env_file:
      - .env
    volumes:
      - /data/postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432" # HOST:CONTAINER
  # redis:
  #   image: redis:5.0
  #   ports:
  #     - "6379:6379"
  #   environment:
  #     - ALLOW_EMPTY_PASSWORD=true
